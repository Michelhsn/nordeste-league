# Nordeste League

This repository contains the static build of the Nordeste League website.

The site is hosted using **GitHub Pages** from the `docs/` folder. To preview
locally, open `docs/index.html` in a browser. After pushing to GitHub, enable
Pages in repository settings with the **Source** set to `Deploy from a branch`
and the `docs/` directory from the `gh-pages` branch.
